<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_nav.xml"
    app:startDestination="@id/bloglist">

    <fragment
        android:id="@+id/bloglist"
        android:name="in.iot.lab.blogengine.Bloglist"
        android:label="fragment_bloglist"
        tools:layout="@layout/fragment_bloglist" >
        <action
            android:id="@+id/action_bloglist_to_blogFragment"
            app:destination="@id/blogFragment" />
    </fragment>
    <fragment
        android:id="@+id/blogFragment"
        android:name="in.iot.lab.blogengine.BlogFragment"
        android:label="fragment_blog"
        tools:layout="@layout/fragment_blog" />
</navigation>